server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tlan-beaconfire/cloud-config
          clone-on-start: true
          skip-ssl-validation: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest